fn main() {
    let t = true;

    let f: bool = false; // with explicit type annotation

    let is_hot: bool = true; // with explicit type annotation

    let is_nice = true;

    let is_valid: bool = false; // with explicit type annotation

    println!("{} {} {} {} {}", t, f, is_hot, is_nice, is_valid);
}
